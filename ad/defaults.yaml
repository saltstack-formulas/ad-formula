# -*- coding: utf-8 -*-
# vim: ft=yaml
---
ad:
  pkg:
    name: ad
  rootgroup: root
  config: '/etc/ad'
  service:
    name: ad
  subcomponent:
    config: '/etc/ad-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
